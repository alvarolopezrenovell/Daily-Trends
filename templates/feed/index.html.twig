{% extends 'base.html.twig' %}

{% block body %}

  <div class="container">
    <div class="row">
      {% for feed in feeds %}
        {% include 'feed/view-min.html.twig' %}
      {% endfor %}
    </div>
  </div>

{% endblock %}
